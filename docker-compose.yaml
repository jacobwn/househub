services:
  postgres:
    image: postgres:18-bookworm
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql
  web:
    expose:
      - "3000"
    depends_on: 
      - postgres
  nginx:
    image: nginx:1.28.1-alpine
    depends_on:
      - web

volumes:
  postgres_data: